# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# PostgreSQL connection string from Neon, Supabase, or local PostgreSQL
# Format: postgresql://username:password@host:port/database?options
# Example: postgresql://user:pass@localhost:5432/mydb
DATABASE_URL=postgresql://username:password@host:port/database

# ===========================================
# ADMIN AUTHENTICATION
# ===========================================
# Admin login credentials for accessing the dashboard
# SECURITY: Change these to secure values and never commit to Git
ADMIN_EMAIL=yashsaxena.personal@gmail.com
ADMIN_PASSWORD=your-secure-password-here
ADMIN_ID=admin-1

# ===========================================
# SESSION SECURITY
# ===========================================
# Secret key for session encryption
# Generate a random string: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# SECURITY: Use a different secret for production
SESSION_SECRET=your-super-secret-session-key-change-this

# ===========================================
# SERVER CONFIGURATION
# ===========================================
# Port for local development (default: 5000)
# Vercel will override this in production
PORT=5000

# Environment mode (development or production)
# Automatically set by Vercel in production
NODE_ENV=development

# Host binding address (default: 0.0.0.0)
# On Windows: Set HOST=localhost to avoid socket binding errors (ENOTSUP)
# On Linux/Mac/Cloud: Use 0.0.0.0 (default, allows external connections)
# HOST=localhost

# ===========================================
# OPTIONAL: VERCEL DEPLOYMENT
# ===========================================
# These are automatically set by Vercel, no need to add manually
# VERCEL=1
# VERCEL_URL=your-project.vercel.app
# VERCEL_ENV=production
